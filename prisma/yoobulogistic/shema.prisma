generator client {
    provider = "prisma-client-js"
    output   = "../generated/prisma/yoobulogistic"
}

datasource db {
    provider = "postgresql"
    url      = env("YOOBU_LOGISTIC_DATABASE_URL")
}
